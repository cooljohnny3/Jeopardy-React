// Modal box for displaying question

import * as React from 'react'

export interface QuesitonModalProps { name: string };

export function QuesitonModal(props: QuesitonModalProps) {
    return <p className="modal"></p>
}